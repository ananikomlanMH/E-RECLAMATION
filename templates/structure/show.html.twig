{% extends 'base.html.twig' %}

{% block title %}Structure{% endblock %}

{% block body %}
    <h1>Structure</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ structure.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ structure.designation }}</td>
            </tr>
            <tr>
                <th>Localisation</th>
                <td>{{ structure.localisation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_structure_index') }}">Revenir à la liste</a>

    <a href="{{ path('app_structure_edit', {'id': structure.id}) }}">Modifier</a>

    {{ include('structure/_delete_form.html.twig') }}
{% endblock %}
